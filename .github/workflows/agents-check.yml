name: Docs Consistency Check

on:
  workflow_dispatch:

jobs:
  docs-check:
    runs-on: ubuntu-latest
    steps:
      - name: Docs CI disabled
        run: echo "Generated docs freshness is no longer enforced in CI."
